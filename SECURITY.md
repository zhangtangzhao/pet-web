# 安全政策

## 支持的版本

| 分支 | 支持情况 |
| ---- | -------- |
| main | 支持安全修复 |

## 报告漏洞

**请勿通过公开 Issue / PR / 讨论区报告安全漏洞。**

请使用 GitHub 的「私下报告漏洞」功能（仓库 Security 标签页 → Report a vulnerability），或联系项目维护者。

报告时请尽量包含：

- 受影响模块与接口（如 `backend/internal/logic/trade`、`/api/pay/notify/wx`）
- 漏洞类型（如越权、注入、支付回调伪造、密钥泄漏）
- 复现步骤或 PoC
- 影响评估

我们会在收到报告后尽快确认并跟进修复。在修复发布前，请对报告内容保密。

## 安全基线（开发注意）

- 所有敏感配置（JWT secret、数据库 / Redis 密码、微信支付密钥、COS 密钥）只经环境变量注入，禁止硬编码或提交；
- 生产模式（`APP_MODE=pro`）启动校验关键配置，`SMS_TEST_CODE` 非空拒绝启动；
- 生产环境 SMS / Redis / PostgreSQL / API 端口仅绑定 `127.0.0.1`，对外仅暴露 Nginx 80/443；
- 支付回调必须验证微信 V3 签名，并以商户订单号 + 状态机保证幂等；
- 默认管理员口令（`admin/admin123456`）与测试验证码（`888888`）仅限开发环境，上线前必须修改。
